//@ https://www.codewars.com/kata/550f22f4d758534c1100025a/train/javascript
//@ 5

function dirReduc(arr) {
  // since we need to recheck the placement of sequential directions after removing one, it might be a good candidate for recursion
  // iterate over the array
  // if i and i+1 are a pair to remove, then remove them, calling the recursive function again with the array
}
